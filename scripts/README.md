# Project utilities

Reserved for small reproducible developer commands such as environment checks, dataset-manifest validation, schema export, fixture loading and demonstration execution.

Keep learning/comparison logic in `src/claim_cmev/` and offline workflows in `pipelines/`. Document each command's input/output paths and prerequisites. Avoid hidden downloads, real approvals or destructive cleanup as startup side effects. No executable utilities are supplied yet.
