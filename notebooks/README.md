# Exploratory notebooks

Use for dataset inspection, visual debugging and experiment exploration. Move reusable conversion/training/evaluation code into the package or offline pipelines.

Record config, source/split IDs and provenance. Clear sensitive/large outputs before committing; keep generated files under ignored `artifacts/`. Notebooks are not production worker entrypoints or the sole source of a reproducible result.
